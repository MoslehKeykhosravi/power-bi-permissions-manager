# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80,http://localhost

# Power BI Report Server Credentials (SECURE - Keep this file private!)
PBI_USERNAME=YOURDOMAIN\youruser
PBI_PASSWORD=your_secure_password
PBI_DOMAIN=YOURDOMAIN
PBI_USER=youruser

# Default Power BI Server URLs (3 predefined servers)
PBI_SERVER_URL_1=https://pbi-server1.yourdomain.com/Power_BI
PBI_SERVER_URL_2=https://pbi-server2.yourdomain.com/Power_BI
PBI_SERVER_URL_3=https://pbi-server3.yourdomain.com/Power_BI

# Active Directory Configuration (Optional - Required for "Select from AD" feature)
# NOTE: You need to find your internal Domain Controller IP address
# The hostname "yourdomain.com" points to public IP and won't work
# Run on Windows: (Get-ADDomainController).HostName to find your DC
# Then use: ldap://YOUR-DC-IP or ldap://dc.yourdomain.local

# Option 1: Using domain controller IP (recommended for Docker)
# LDAP_URL=ldap://192.168.1.10

# Option 2: Using email format (if reachable)
LDAP_URL=ldap://yourdomain.com
LDAP_BIND_DN=youruser@yourdomain.com
LDAP_BIND_PASSWORD=your_secure_password
LDAP_SEARCH_BASE=DC=yourdomain,DC=com

# Alternative BIND DN format:
# LDAP_BIND_DN=CN=youruser,CN=Users,DC=yourdomain,DC=com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

